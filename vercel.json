{
  "buildCommand": "cd fantasy-bball-app && npm run build",
  "outputDirectory": "fantasy-bball-app/.next",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ]
}
